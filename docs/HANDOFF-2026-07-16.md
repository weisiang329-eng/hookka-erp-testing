# Handoff — 2026-07-16

Two tasks are specced here, ready to pick up. Everything else from this session is shipped
and verified (see the bottom).

**Owner ruling: Phase 3 (delivery "wait-until-full" packing) is NOT to be started.**
Everything else may proceed.

---

## Task 1 — Impersonation ("login as user")  ⚠️ auth-sensitive

**Why:** we have RBAC, departments, a mobile portal and per-role route gates — and no way to
see the app as another user. Today, *"the sewing supervisor's Planning page is blank"* is not
reproducible without that person's password. Confirmed: no `impersonate` / `loginAs` / `actAs`
exists anywhere in the repo.

**Reference implementation:** the owner's other repo `weisiang329-eng/houzs-erp` has a careful
one — copy the REASONING, not its D1 SQL:
`functions/api/auth/impersonate.ts`, `stop-impersonate.ts`, `functions/_auth.ts` (the `imp`
JWT claim), `src/components/layout/impersonate-banner.tsx`.

### Security invariants (all five are load-bearing — do not drop any)
1. **Re-verify the original admin FROM THE DB, not from the token claim**, on every switch.
   A stale/forged claim must never grant admin.
2. **Short TTL** — 2h, vs the normal session length. An impersonated session must not linger.
3. **Do NOT update the target's `last_login`** — admin activity must never masquerade as the
   real user's.
4. **Audit as the ADMIN**, not the target: `impersonate_start` / `impersonate_stop` attributed
   to the real actor. Use `emitAudit` (`src/api/lib/audit.ts`).
5. **Collapse "user not found" and "user disabled" into ONE generic error**
   (`Invalid impersonation target`) so an admin can't probe which user ids exist.

### Our auth stack — what it must plug into (read ALL of these first)
- `src/api/lib/session-cookie.ts` — how a session token is minted/parsed.
- `src/api/lib/auth-middleware.ts` — resolves user + role per request. **Note the session
  CACHE**: `sessionCacheKey()`, `invalidateSessionCache()`, `purgeUserSessions()`. Impersonation
  must interact with this correctly or you'll serve a cached identity to the wrong session.
- `src/api/routes/auth.ts` (+ `auth-oauth.ts`, `auth-totp.ts`) — the existing login paths.
- `src/api/lib/rbac.ts` — `requireSuperAdmin(c)` gates the 7 user mutations; impersonation
  start must sit behind it.
- `src/api/lib/auth-wireup-notes.md` — read before touching any of the above.

### Suggested shape
- `POST /api/auth/impersonate {userId}` — `requireSuperAdmin`; re-verify admin from DB; mint a
  session for the target carrying the original admin id; 2h TTL; audit as admin.
- `POST /api/auth/stop-impersonate` — restore the admin's own session from the carried id
  (re-verified from the DB again).
- An amber top banner — "X impersonating Y" + **Return to admin** — so it is impossible to
  forget you're impersonating. Rendered in `src/layouts/DashboardLayout.tsx`.

### Why it wasn't done in-session
It has to be threaded through session minting + the middleware + the session cache. A rushed
version risks privilege escalation. It needs someone who has read the whole auth path.

---

## Task 2 — /invoices page jank (328 ms)  ✅ safe, well-measured

**Measured on prod 2026-07-16** (Long Task API, after the chunk prefetch was already warm — so
this is render cost, NOT download):

| Page | Main thread blocked | Worst single block |
|---|---|---|
| **/invoices** | **328 ms** | **228 ms** ← worst in the app |
| /delivery | 219 ms | 143 ms |
| /inventory | 216 ms | 216 ms |
| /customers | 185 ms | 113 ms |
| **/planning** | **0 ms** | 0 ← proof it's avoidable |

**How to reproduce** (any authed browser console, then click through pages):
```js
window.__lag = [];
new PerformanceObserver(l => { for (const e of l.getEntries())
  window.__lag.push({ ms: Math.round(e.duration), t: Math.round(e.startTime) });
}).observe({ entryTypes: ['longtask'] });
// …navigate, then read window.__lag
```

**What's already done (don't redo):**
- Tab switches on the monolith pages are wrapped in `React.startTransition` — the block is
  interruptible, so rapid clicking stays responsive. The block itself is still there.
- `src/lib/prefetch-routes.ts` warms the main chunks on idle, so first-visit download is not
  the cause.
- `useCachedJson` (`src/lib/cached-fetch.ts`) already aborts + dedups in-flight requests on
  rapid key change — stacked fetches are NOT the cause either.

**So the remaining 328 ms is synchronous work in the initial render.** Likely suspects: the
grid mounting all rows at once, and per-row derivation in the render path. `/planning` scoring
0 ms is the proof this is the page's own doing.

**Approach:** profile the invoices page render (React DevTools Profiler or a `performance.mark`
around the grid), then either virtualise the grid or move the per-row derivation into a memo
keyed on the data (not on the tab). Do not "optimise" blind — get the flame chart first.

**The durable fix behind all of these** is decomposing the monolith pages so a tab switch
re-renders only the active tab: `src/pages/employees.tsx` (~11k lines),
`src/pages/production/index.tsx` (~9.6k), `src/pages/delivery/index.tsx` (~7.2k). That is a
big, separate piece of work.

---

## Also open (not started)

- **Root cause of the 35 duplicate invoices.** We cancelled them (correctly — cancel keeps the
  audit trail; deleting an issued invoice is an LHDN risk) but never chased HOW they were
  created, or whether the later double-invoice guard actually closed that path. If it didn't,
  they will grow back. **Note:** the duplicate-*SO* guard (`sales-orders.ts:2018`, blocks a
  repeat customer PO per customer with a 409 naming the existing SO) is a DIFFERENT guard —
  don't confuse the two.
- **`cs-agent.ts:503`** runs a FULL `computeChainWithAssignments` per customer delivery-date
  question — the whole planning engine on an interactive path.
- **Engine leftovers** (not urgent, it's 5.1 s now): the generate path builds all 8 department
  snapshots (~30k cells) then throws them away — `computeChainWithAssignments` only reads the
  `collect` callback; and `renderSewing`'s `sewBlockedMin` is a real O(days×cards) with a regex
  in the body. Also: count+supersede in `schedule-proposals.ts` could collapse into one
  `UPDATE … RETURNING`.
- **Invoice "total gap drawer" price bug** — parked, needs the owner to point at a specific
  invoice.
- `src/pages/consignment/note.tsx` never got the `startTransition` tab fix (its tab bar is
  shaped differently).

---

## Shipped + verified this session (prod)

- **The big one: the schedule engine went from dying on every run to 5.1 s.** It had been
  killed mid-run since 15 Jul (five zombie `status='running'` rows) while the console showed
  PHASE 3 · FULL-AUTO and the queue grew to 1,715. Root cause was **~114 strictly serialized DB
  round-trips** (`CHUNK = 40`, three sequential queries per chunk), not the engine —
  `computeChain` runs in tens of ms. Fix: `CHUNK 40 → 500` (bind ceiling is 65535/11 ≈ 5,900
  rows, so 40 was 100× over-conservative).
  **The lesson worth keeping: Workers CPU time EXCLUDES I/O wait, so a query storm can never
  trip a CPU limit. When a Worker run hangs for minutes, suspect I/O round-trips, not CPU.**
  (Two wrong diagnoses preceded this — "shared CPU budget" and "quadratic loop" — both were
  shipped and both changed nothing. Don't retry either.)
- Due dates now actually get written (drain moved to the top of the beat, runs alone).
- The drain no longer clocks off at 8pm — it only finishes work the engine already decided, so
  an overnight queue is done by morning. The 8pm gate stays on everything that *reasons*.
- Stuck-run reaper (>15 min at 'running' → errored) — this is what exposed the five zombies.
- GitHub cron does not honour schedules: `'0 * * * *'` actually fired every 2–3 hours. Now
  `'7,27,47 * * * *'` (off-peak minutes). `gh workflow run agent-heartbeat.yml` fires one on
  demand.
- **Employee Agent** + **Service Agent** — new, live, both producing real daily digests.
- 3-phase automation selector (Propose / Auto-tune / Full-auto) on every lifecycle agent; CS is
  single-mode and correctly has none.
- **Delivery Return: complete and merged to prod** (9a00cad0).
- Route prefetch, `startTransition` on the monolith tab bars.
